import { createStyles } from "@/theme/createStyles";

/**
 * Estilos del paso de ánimo/cuerpo. El lienzo lo aporta `CheckinScreen`;
 * aquí solo se reservan estilos puntuales. Actualmente vacío intencionalmente.
 */
export const useFeelingsStyles = createStyles(() => ({}));
