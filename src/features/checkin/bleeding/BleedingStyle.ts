import { createStyles } from "@/theme/createStyles";

/**
 * Estilos del paso de sangrado. El lienzo (SafeAreaView + ScrollView + footer)
 * lo aporta `CheckinScreen`; aquí solo se reservan estilos puntuales si algún
 * paso los necesita. Actualmente vacío intencionalmente.
 */
export const useBleedingStyles = createStyles(() => ({}));
