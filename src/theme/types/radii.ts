/** Radios de esquina del sistema visual. */
export type Radius = {
    none: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
    "2xl": number;
    "3xl": number;
    pill: number;
    full: number;
};

/** Anchos de borde disponibles para la UI. */
export type BorderWidth = {
    hairline: number;
    thin: number;
    thick: number;
};
